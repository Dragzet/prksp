# Практическая работа №6
## Аутентификация и авторизация в React-приложениях

### Описание
Это React-приложение демонстрирует реализацию безопасной аутентификации с использованием **PKCE Flow (Proof Key for Code Exchange)** и системы авторизации на основе ролей и прав доступа.

### Основные возможности

#### 🔐 PKCE Flow
- Генерация `code_verifier` и `code_challenge`
- Трехшаговый процесс аутентификации:
  1. Инициация PKCE (генерация challenge)
  2. Получение кода авторизации
  3. Обмен кода на токен доступа

#### 👥 Система ролей
Приложение поддерживает три типа пользователей:
- **admin** - полный доступ ко всем разделам
- **editor** - доступ к редактированию статей
- **user** - базовый доступ для просмотра

#### 🛡️ Система прав (permissions)
- `can_view_articles` - просмотр статей
- `can_edit_articles` - редактирование статей
- `can_delete_articles` - удаление статей
- `can_manage_users` - управление пользователями

### Тестовые учетные записи

| Username | Password   | Роли           | Права доступа                                                         |
|----------|------------|----------------|-----------------------------------------------------------------------|
| admin    | admin123   | admin, user    | все права                                                             |
| editor   | editor123  | editor, user   | can_view_articles, can_edit_articles                                  |
| user     | user123    | user           | can_view_articles                                                     |

### Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build
```

После запуска приложение будет доступно по адресу: http://localhost:3000

### Структура проекта

```
prac6/
├── src/
│   ├── components/          # React компоненты
│   │   ├── LoginForm.jsx    # Форма входа
│   │   ├── ProtectedRoute.jsx   # Защищенный маршрут
│   │   └── RoleBasedRoute.jsx   # Маршрут с проверкой ролей
│   ├── context/             # React Context
│   │   └── AuthContext.jsx  # Контекст аутентификации
│   ├── pages/               # Страницы приложения
│   │   ├── Home.jsx         # Главная страница
│   │   ├── Articles.jsx     # Страница статей
│   │   ├── Editor.jsx       # Страница редактора
│   │   ├── Admin.jsx        # Админ-панель
│   │   └── Forbidden.jsx    # Страница "Доступ запрещен"
│   ├── services/            # Сервисы
│   │   └── authService.js   # Сервис аутентификации
│   ├── utils/               # Утилиты
│   │   ├── auth.js          # Утилиты авторизации
│   │   └── pkce.js          # Утилиты PKCE
│   ├── App.jsx              # Главный компонент
│   ├── main.jsx             # Точка входа
│   └── index.css            # Глобальные стили
├── index.html
├── package.json
└── vite.config.js
```

### Реализованные компоненты

#### AuthContext
Предоставляет глобальный доступ к состоянию аутентификации:
- `user` - текущий пользователь
- `loading` - состояние загрузки
- `error` - ошибки аутентификации
- `login()` - функция входа
- `logout()` - функция выхода
- `isAuthenticated` - флаг аутентификации

#### ProtectedRoute
Компонент для защиты маршрутов, требующих аутентификации. Если пользователь не авторизован, перенаправляет на страницу входа.

#### RoleBasedRoute
Компонент для защиты маршрутов с проверкой ролей. Если у пользователя нет необходимой роли, перенаправляет на страницу "Доступ запрещен".

### Маршруты приложения

| Маршрут   | Защита         | Требуемые роли  | Описание                   |
|-----------|----------------|-----------------|----------------------------|
| /login    | Публичный      | -               | Страница входа             |
| /         | Аутентификация | любая           | Главная страница           |
| /articles | Аутентификация | любая           | Просмотр статей            |
| /editor   | Роли           | editor, admin   | Редактирование статей      |
| /admin    | Роли           | admin           | Администрирование          |
| /forbidden| Публичный      | -               | Доступ запрещен            |

### Технологии

- **React 18** - библиотека для создания пользовательских интерфейсов
- **React Router 6** - маршрутизация
- **Vite** - сборщик и dev-сервер
- **Web Crypto API** - криптографические операции для PKCE
- **LocalStorage** - хранение токенов и пользовательских данных

### Безопасность

Приложение демонстрирует следующие практики безопасности:
- ✅ PKCE Flow для защиты от перехвата кода авторизации
- ✅ Хеширование code_verifier с помощью SHA-256
- ✅ Проверка срока действия токенов
- ✅ Разделение прав доступа по ролям
- ✅ Защита маршрутов от неавторизованного доступа
- ✅ Условный рендеринг UI на основе прав пользователя

### Примечания

Это учебное приложение для демонстрации концепций аутентификации и авторизации. В реальном production приложении необходимо:
- Использовать настоящий OAuth2/OIDC сервер
- Хранить токены в httpOnly cookies
- Реализовать refresh token механизм
- Добавить CSRF защиту
- Использовать HTTPS
- Валидировать токены на backend

### Автор
Практическая работа №6 по курсу разработки React-приложений

### Лицензия
MIT
