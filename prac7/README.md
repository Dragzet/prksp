# CRUD Приложение на React + PostgreSQL

Практическая работа №7 - Создание CRUD приложения для интернет-магазина

## 📋 Описание проекта

Полноценное CRUD приложение для управления товарами интернет-магазина с использованием:
- **Frontend**: React 18, React Router, Axios
- **Backend**: Node.js, Express
- **Database**: PostgreSQL
- **ORM**: Sequelize

## 🎯 Реализованные CRUD операции

### CREATE (Создание)
- Добавление нового товара через форму
- HTTP POST запрос на `/api/products`
- Валидация обязательных полей (название, цена)

### READ (Чтение)
- Просмотр списка всех товаров
- Детальный просмотр отдельного товара
- HTTP GET запросы на `/api/products` и `/api/products/:id`

### UPDATE (Обновление)
- Редактирование информации о товаре
- HTTP PUT запрос на `/api/products/:id`
- Сохранение изменений в базе данных

### DELETE (Удаление)
- Удаление товара из базы данных
- HTTP DELETE запрос на `/api/products/:id`
- Подтверждение перед удалением

## 📁 Структура проекта

```
prac7/
├── backend/                    # Серверная часть
│   ├── config/
│   │   └── database.js        # Конфигурация PostgreSQL
│   ├── controllers/
│   │   └── productController.js   # Контроллеры CRUD операций
│   ├── models/
│   │   └── Product.js         # Модель товара (Sequelize)
│   ├── routes/
│   │   └── productRoutes.js   # API маршруты
│   ├── .env                   # Переменные окружения
│   ├── package.json
│   └── server.js             # Точка входа сервера
│
└── frontend/                  # Клиентская часть
    ├── public/
    │   └── index.html
    ├── src/
    │   ├── components/
    │   │   ├── ProductList.js     # Список товаров (READ)
    │   │   ├── ProductList.css
    │   │   ├── ProductForm.js     # Форма создания (CREATE)
    │   │   ├── ProductForm.css
    │   │   ├── ProductEdit.js     # Форма редактирования (UPDATE)
    │   │   ├── ProductView.js     # Просмотр товара (READ)
    │   │   └── ProductView.css
    │   ├── services/
    │   │   └── api.js            # API сервис (Axios)
    │   ├── App.js                # Главный компонент
    │   ├── App.css
    │   ├── index.js
    │   └── index.css
    └── package.json
```

## 🚀 Установка и запуск

### Предварительные требования

1. **Node.js** (v16 или выше)
2. **PostgreSQL** (v12 или выше)
3. **npm** или **yarn**

### Шаг 1: Настройка базы данных PostgreSQL

1. Установите PostgreSQL, если еще не установлен
2. Создайте базу данных:

```sql
CREATE DATABASE crud_db;
```

3. Настройте пользователя и пароль (или используйте существующие)

### Шаг 2: Настройка Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте файл `.env`:
```env
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=ваш_пароль
DB_NAME=crud_db
DB_PORT=5432

PORT=5000
```

4. Запустите сервер:
```bash
npm start
```

Сервер запустится на `http://localhost:5000`

### Шаг 3: Настройка Frontend

1. Откройте новый терминал и перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите React приложение:
```bash
npm start
```

Приложение откроется в браузере на `http://localhost:3000`

## 📡 API Endpoints

| Метод  | Endpoint              | Описание                    |
|--------|-----------------------|-----------------------------|
| GET    | `/api/products`       | Получить все товары         |
| GET    | `/api/products/:id`   | Получить товар по ID        |
| POST   | `/api/products`       | Создать новый товар         |
| PUT    | `/api/products/:id`   | Обновить товар              |
| DELETE | `/api/products/:id`   | Удалить товар               |

### Пример тела запроса (POST/PUT):

```json
{
  "name": "Ноутбук ASUS",
  "description": "Мощный игровой ноутбук",
  "price": 89999.99,
  "quantity": 5,
  "category": "Электроника",
  "imageUrl": "https://example.com/laptop.jpg"
}
```

## 🗄️ Модель данных (Product)

| Поле        | Тип          | Описание                 | Обязательное |
|-------------|--------------|--------------------------|--------------|
| id          | INTEGER      | Автоинкремент, PK        | Да           |
| name        | STRING       | Название товара          | Да           |
| description | TEXT         | Описание товара          | Нет          |
| price       | DECIMAL(10,2)| Цена товара              | Да           |
| quantity    | INTEGER      | Количество на складе     | Да           |
| category    | STRING       | Категория товара         | Нет          |
| imageUrl    | STRING       | URL изображения          | Нет          |
| createdAt   | TIMESTAMP    | Дата создания            | Авто         |
| updatedAt   | TIMESTAMP    | Дата обновления          | Авто         |

## 🎨 Функционал приложения

### 1. Главная страница (/)
- Отображение всех товаров в виде карточек
- Кнопки для просмотра, редактирования и удаления каждого товара
- Кнопка "Добавить товар"

### 2. Добавление товара (/add)
- Форма с полями: название, описание, цена, количество, категория, изображение
- Валидация обязательных полей
- Сохранение в базу данных

### 3. Просмотр товара (/view/:id)
- Детальная информация о товаре
- Изображение товара (если есть)
- Метаданные (дата создания, обновления)

### 4. Редактирование товара (/edit/:id)
- Предзаполненная форма с текущими данными
- Обновление информации о товаре
- Сохранение изменений

## 🔧 Технологии и библиотеки

### Backend
- **Express** - веб-фреймворк
- **Sequelize** - ORM для работы с PostgreSQL
- **pg** - PostgreSQL клиент для Node.js
- **cors** - поддержка CORS
- **dotenv** - управление переменными окружения

### Frontend
- **React** - UI библиотека
- **React Router** - навигация между страницами
- **Axios** - HTTP клиент для API запросов
- **CSS3** - стилизация компонентов

## 🧪 Тестирование

### Тестирование API через curl:

```bash
# Получить все товары
curl http://localhost:5000/api/products

# Создать товар
curl -X POST http://localhost:5000/api/products \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовый товар",
    "price": 1000,
    "quantity": 10
  }'

# Обновить товар
curl -X PUT http://localhost:5000/api/products/1 \
  -H "Content-Type: application/json" \
  -d '{"price": 1500}'

# Удалить товар
curl -X DELETE http://localhost:5000/api/products/1
```

## 📝 Возможные проблемы и решения

### 1. Ошибка подключения к PostgreSQL
- Проверьте правильность данных в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя

### 2. CORS ошибки
- Убедитесь, что `cors` middleware включен в `server.js`
- Проверьте, что backend запущен на порту 5000

### 3. Ошибка "Cannot find module"
- Выполните `npm install` в соответствующей папке
- Удалите `node_modules` и `package-lock.json`, затем переустановите

## 📚 Дополнительные материалы

- [Express.js документация](https://expressjs.com/)
- [Sequelize документация](https://sequelize.org/)
- [React документация](https://react.dev/)
- [PostgreSQL документация](https://www.postgresql.org/docs/)

## 👨‍💻 Автор

Практическая работа №7 - CRUD приложение на React

## 📄 Лицензия

MIT License

---

**Примечание**: Это учебный проект для демонстрации CRUD операций. Для production использования требуется добавить:
- Аутентификацию и авторизацию
- Валидацию на стороне сервера
- Обработку загрузки файлов
- Пагинацию
- Поиск и фильтрацию
- Обработку ошибок
- Логирование
- Тесты
