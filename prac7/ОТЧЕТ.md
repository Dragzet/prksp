# Отчет по практической работе №7
## Создание CRUD приложения на React

**Студент:** _______________________  
**Группа:** _______________________  
**Дата:** 14.02.2026

---

## 1. Цель работы

Создать React приложение с базой данных PostgreSQL и реализовать запись и удаление информации из базы данных в соответствии с полученными HTTP запросами.

## 2. Задачи

1. Настроить PostgreSQL базу данных
2. Создать серверную часть на Node.js/Express с Sequelize ORM
3. Реализовать REST API для CRUD операций
4. Создать клиентскую часть на React
5. Реализовать все CRUD операции:
   - **Create** - создание товара
   - **Read** - чтение/просмотр товаров
   - **Update** - обновление товара
   - **Delete** - удаление товара

## 3. Выполнение работы

### 3.1. Структура проекта

Проект разделен на две части:
- **backend/** - серверная часть (Node.js + Express + PostgreSQL)
- **frontend/** - клиентская часть (React)

### 3.2. Backend (Серверная часть)

#### 3.2.1. Использованные технологии:
- Node.js
- Express.js - веб-фреймворк
- PostgreSQL - база данных
- Sequelize - ORM
- CORS - для кросс-доменных запросов

#### 3.2.2. Реализованная модель данных (Product):

```javascript
{
  id: INTEGER (Primary Key, Auto Increment),
  name: STRING (обязательное),
  description: TEXT,
  price: DECIMAL(10,2) (обязательное),
  quantity: INTEGER,
  category: STRING,
  imageUrl: STRING,
  createdAt: TIMESTAMP,
  updatedAt: TIMESTAMP
}
```

#### 3.2.3. API Endpoints:

| HTTP Метод | Endpoint | Описание | CRUD операция |
|------------|----------|----------|---------------|
| POST | /api/products | Создание товара | CREATE |
| GET | /api/products | Получение всех товаров | READ |
| GET | /api/products/:id | Получение товара по ID | READ |
| PUT | /api/products/:id | Обновление товара | UPDATE |
| DELETE | /api/products/:id | Удаление товара | DELETE |

### 3.3. Frontend (Клиентская часть)

#### 3.3.1. Использованные технологии:
- React 18
- React Router - маршрутизация
- Axios - HTTP клиент

#### 3.3.2. Реализованные компоненты:

1. **ProductList** - отображение списка товаров
   - Получение данных через GET запрос
   - Кнопки для просмотра, редактирования, удаления

2. **ProductForm** - форма создания товара
   - Отправка данных через POST запрос
   - Валидация обязательных полей

3. **ProductEdit** - форма редактирования товара
   - Загрузка данных через GET запрос
   - Обновление через PUT запрос

4. **ProductView** - детальный просмотр товара
   - Отображение всей информации о товаре
   - Переход к редактированию

### 3.4. Реализация CRUD операций

#### CREATE (Создание):
```javascript
// Frontend: services/api.js
export const createProduct = async (productData) => {
  const response = await axios.post(API_URL, productData);
  return response.data;
};

// Backend: controllers/productController.js
exports.createProduct = async (req, res) => {
  const product = await Product.create(req.body);
  res.status(201).json(product);
};
```

#### READ (Чтение):
```javascript
// Frontend
export const getAllProducts = async () => {
  const response = await axios.get(API_URL);
  return response.data;
};

// Backend
exports.getAllProducts = async (req, res) => {
  const products = await Product.findAll();
  res.json(products);
};
```

#### UPDATE (Обновление):
```javascript
// Frontend
export const updateProduct = async (id, productData) => {
  const response = await axios.put(`${API_URL}/${id}`, productData);
  return response.data;
};

// Backend
exports.updateProduct = async (req, res) => {
  const product = await Product.findByPk(id);
  await product.update(req.body);
  res.json(product);
};
```

#### DELETE (Удаление):
```javascript
// Frontend
export const deleteProduct = async (id) => {
  const response = await axios.delete(`${API_URL}/${id}`);
  return response.data;
};

// Backend
exports.deleteProduct = async (req, res) => {
  const product = await Product.findByPk(id);
  await product.destroy();
  res.json({ message: 'Товар удален' });
};
```

## 4. Тестирование

### 4.1. Тестирование CREATE операции:
1. Открыта форма добавления товара
2. Заполнены поля: название, описание, цена, количество
3. Нажата кнопка "Создать товар"
4. Товар успешно добавлен в базу данных
5. Выполнен редирект на главную страницу

**Результат:** ✅ Успешно

### 4.2. Тестирование READ операции:
1. Открыта главная страница
2. Выполнен GET запрос к /api/products
3. Получен список всех товаров из БД
4. Товары отображены в виде карточек

**Результат:** ✅ Успешно

### 4.3. Тестирование UPDATE операции:
1. Нажата кнопка "Изменить" на товаре
2. Открыта форма редактирования с текущими данными
3. Изменена цена товара
4. Нажата кнопка "Сохранить изменения"
5. Данные обновлены в БД

**Результат:** ✅ Успешно

### 4.4. Тестирование DELETE операции:
1. Нажата кнопка "Удалить" на товаре
2. Появилось окно подтверждения
3. Подтверждено удаление
4. Товар удален из БД и интерфейса

**Результат:** ✅ Успешно

## 5. Скриншоты работы приложения

### 5.1. Главная страница со списком товаров
_[Место для скриншота]_

### 5.2. Форма добавления товара
_[Место для скриншота]_

### 5.3. Детальный просмотр товара
_[Место для скриншота]_

### 5.4. Форма редактирования товара
_[Место для скриншота]_

## 6. Возникшие проблемы и их решения

### Проблема 1: CORS ошибка при запросах к API
**Решение:** Добавлен middleware `cors()` в Express сервер

### Проблема 2: Ошибка подключения к PostgreSQL
**Решение:** Настроены правильные параметры подключения в файле .env

### Проблема 3: Не обновлялся список после добавления товара
**Решение:** Добавлен редирект на главную страницу после успешного создания

## 7. Выводы

В ходе выполнения практической работы:

1. Успешно создано полноценное CRUD приложение на React
2. Реализованы все четыре базовые операции с данными:
   - **CREATE** - создание нового товара через форму
   - **READ** - просмотр списка и отдельных товаров
   - **UPDATE** - редактирование существующих товаров
   - **DELETE** - удаление товаров из базы данных

3. Настроено взаимодействие между React фронтендом и Node.js бэкендом через REST API

4. Использован Sequelize ORM для работы с PostgreSQL базой данных

5. Реализована маршрутизация с помощью React Router

6. Применен Axios для выполнения HTTP запросов

7. Создан современный пользовательский интерфейс с адаптивным дизайном

Приложение полностью функционально и готово к демонстрации. Все CRUD операции работают корректно, данные успешно сохраняются и извлекаются из базы данных PostgreSQL.

## 8. Ответы на контрольные вопросы

### 1. Что такое CRUD?
CRUD - это акроним четырех базовых операций с данными:
- **C**reate - создание
- **R**ead - чтение
- **U**pdate - обновление
- **D**elete - удаление

### 2. Какие HTTP методы соответствуют CRUD операциям?
- CREATE → POST
- READ → GET
- UPDATE → PUT/PATCH
- DELETE → DELETE

### 3. Что такое REST API?
REST (Representational State Transfer) API - это архитектурный стиль для создания веб-сервисов, использующий HTTP протокол и стандартные методы (GET, POST, PUT, DELETE) для взаимодействия с ресурсами.

### 4. Для чего используется Sequelize?
Sequelize - это ORM (Object-Relational Mapping) для Node.js, который позволяет работать с базами данных через объекты JavaScript вместо SQL запросов.

### 5. Почему важно понимать CRUD?
CRUD операции - это основа большинства приложений. Понимание CRUD позволяет правильно проектировать API, работать с базами данных и создавать полноценные приложения.

---

**Подпись студента:** _______________________  
**Дата:** 14.02.2026
